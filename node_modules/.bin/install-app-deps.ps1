#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
$pathsep=":"
$env_node_path=$env:NODE_PATH
$new_node_path="D:\deepseek-harness\node_modules\.pnpm\electron-builder@25.0.0_ele_a952eb6192fdc6870aeee9707d26c218\node_modules\electron-builder\node_modules;D:\deepseek-harness\node_modules\.pnpm\electron-builder@25.0.0_ele_a952eb6192fdc6870aeee9707d26c218\node_modules;D:\deepseek-harness\node_modules\.pnpm\node_modules"
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
  $pathsep=";"
} else {
  $new_node_path="/mnt/d/deepseek-harness/node_modules/.pnpm/electron-builder@25.0.0_ele_a952eb6192fdc6870aeee9707d26c218/node_modules/electron-builder/node_modules:/mnt/d/deepseek-harness/node_modules/.pnpm/electron-builder@25.0.0_ele_a952eb6192fdc6870aeee9707d26c218/node_modules:/mnt/d/deepseek-harness/node_modules/.pnpm/node_modules"
}
if ([string]::IsNullOrEmpty($env_node_path)) {
  $env:NODE_PATH=$new_node_path
} else {
  $env:NODE_PATH="$new_node_path$pathsep$env_node_path"
}

$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/node$exe"  "$basedir/../electron-builder/install-app-deps.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../electron-builder/install-app-deps.js" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "node$exe"  "$basedir/../electron-builder/install-app-deps.js" $args
  } else {
    & "node$exe"  "$basedir/../electron-builder/install-app-deps.js" $args
  }
  $ret=$LASTEXITCODE
}
$env:NODE_PATH=$env_node_path
exit $ret
